# Feature Specification: M5 - Eval harness and public benchmark

**Feature Branch**: `016-m5-eval-harness-and-benchmark`
**Created**: 2026-09-10
**Status**: Stub
**Input**: docs/SCTXX-ROADMAP.md M5; docs/SPEC.md §15.2

## Scope seed

- `sctxx eval` with baselines, held-out continuation test, public corpus, reproducible README table, nightly gate, dataset export.

## Unlocked by

- No open Wayfinder ticket; unlocked when the previous milestone's exit criterion is met.

## Next step

Not approved scope. When unlocked, set `.specify/feature.json` to `specs/016-m5-eval-harness-and-benchmark` and run
`/speckit-specify` to replace this stub with a full specification.
