# Feature Specification: M2 - Pi adapter

**Feature Branch**: `007-m2-pi-adapter`
**Created**: 2026-09-10
**Status**: Stub
**Input**: docs/SCTXX-ROADMAP.md M2; docs/SPEC.md §6.1, §6.4

## Scope seed

- Session versions 1–3, header and entry types, tree walk from the leaf, branch summaries, compaction entries as low-trust seeds.

## Unlocked by

- No open Wayfinder ticket; unlocked when the previous milestone's exit criterion is met.

## Next step

Not approved scope. When unlocked, set `.specify/feature.json` to `specs/007-m2-pi-adapter` and run
`/speckit-specify` to replace this stub with a full specification.
