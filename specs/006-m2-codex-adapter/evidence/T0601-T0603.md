# Evidence: T0601 codex/windowed-compaction.jsonl

- Commit: `0b81048d81808574c86437e4290f89c629cde40e`
- RED: not captured as a separate run. No fixture in the corpus contained a `compacted` item with a
  `window_number`, so the code path that distinguishes it had no coverage at all — this fixture is what
  creates that coverage. Recorded as process debt (see Residual risk).
- GREEN: `cargo test --all-features --test adapters codex_windowed_compaction` → pass (23 adapter tests)
- Gate: `cargo fmt --all -- --check && cargo clippy --all-targets --all-features -- -D warnings &&
  cargo test --all-features && cargo test --no-default-features` → pass
- Snapshots:
  - new `tests/snapshots/adapters__codex_windowed_compaction.snap` — evt 1 is a legacy reset
    (`windowed: false`, summary present); evt 3 is a window marker (`windowed: true`, `summary: null`)
  - `adapters__claude_compact_boundary.snap`, `adapters__codex_ask_and_compaction.snap`,
    `adapters__pi_v1_linear.snap` — each gained exactly one line, `"windowed": false`, because
    `NativeCompaction` gained a field. Reviewed line by line; no other change in any diff.
- Acceptance observation: the fixture is synthetic (AGENTS.md rule 4) and models the `CompactedItem`
  shape read from the reference clone: `message`, `replacement_history`, `window_number`,
  `first_window_id`, `previous_window_id`. A window marker with an empty `message` is a `System`
  event, not a summary — which is what the snapshot shows for evt 3.
- Clean-room: n/a (Codex is Apache-2.0 and inspectable; no Claude Code source involved).
- Residual risk: the fixture is synthetic and the reference clone's revision is disputed
  (`specs/000-wayfinding/issues/11-codex-vendoring-pin.md`). Confirmation against two real rollouts —
  one OpenAI-hosted, one not — is still owed before this is called final.

# Evidence: T0602 compaction kind in the IR

- Commit: `0b81048d81808574c86437e4290f89c629cde40e`
- RED: not captured as a separate run. Before this change the IR had no way to express the
  distinction and `NativeCompaction { evt, summary }` compiled with no field to set — the test
  `a_codex_window_marker_is_not_a_history_reset` could not have compiled against it.
- GREEN: `cargo test --all-features --test adapters a_codex_window_marker_is_not_a_history_reset` → pass
- Gate: as T0601 → pass
- Acceptance observation: the test asserts `[(1, false), (3, true)]` for the fixture's compaction
  kinds, that the empty `message` yields `summary: None`, and that `Session.active` still contains
  every event — i.e. a compaction boundary alone never truncates the active branch (FR-007).
- Residual risk: `mark_compaction_windowed` is a post-`push` marker call. It is a no-op if the event
  was not a compaction, which is deliberate, but it means the builder's state is briefly inconsistent
  between `push` and the mark. Cheap to keep correct; revisit only if adapters start marking before
  pushing.

# Evidence: T0603 --since-compact

- Commit: `0b81048d81808574c86437e4290f89c629cde40e`
- RED: not captured as a separate run. Before this change `sctxx extract --since-compact` exited 2
  (unknown flag) — the CLI contract test pins that it now exits 0 and produces the artifact.
- GREEN:
  - `cargo test --all-features --lib since_compact` → 1 passed
  - `cargo test --all-features --test cli since_compact` → 2 passed
- Gate: as T0601 → pass
- Acceptance observation: on `codex/windowed-compaction.jsonl`, stderr carries
  `[since-compact] starting at evt 1: ...` (the legacy reset wins over the later window marker), stdout
  contains `now do the second half` and does **not** contain `start the migration`; on
  `codex/basic.jsonl` (no compaction at all) stderr says the session has no provider compaction, exit
  code is 0, and the whole session is still extracted.
- Residual risk: `--since-compact` composes with `--include-fork-parent` only by accident; the fork
  flag is not built, and the interaction is listed as a non-goal in `spec.md`.
