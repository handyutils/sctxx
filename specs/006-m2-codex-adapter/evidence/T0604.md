# Evidence: T0604 provider summaries as fold seeds

- Commit: `58c4315c65485cab1225157adb1b8b6afc64c178`
- RED: not captured as a separate failing run. Before this change there was no
  `{{prior_summaries}}` placeholder and no seed block, so
  `the_low_trust_seed_reaches_the_rendered_fold_prompt` could not have compiled against the old
  `Fields`. Recorded as the same process debt as T0601–T0603.
- GREEN:
  - `cargo test --all-features --lib prior_summaries` → 2 passed
  - `cargo test --all-features --lib the_low_trust_seed` → 1 passed
  - `cargo test --all-features --lib every_placeholder` → 1 passed
- Gate: `RUSTFLAGS=-D warnings` + fmt, clippy, `test --all-features` (203 + 23 + 28 + 16 + 1),
  `test --no-default-features` (197 + 22 + 28 + 16 + 1), vendor headers → all pass
- Snapshots: `pipeline__handoff_deterministic_{claude,codex,pi}.snap` each changed by exactly one
  line, `fold_user: 1` → `fold_user: 2`, because the artifact records the prompt versions it used.
  Diffed and reviewed individually; no other line moved.
- Acceptance observation:
  - A chunk starting before any compaction renders `None before this point.`
  - A chunk after the legacy reset at evt 1 carries that summary; a chunk after the window marker at
    evt 3 carries both, with the marker reported as
    `(the provider recorded this boundary but left no readable summary)`.
  - With six summaries and a chunk at evt 100, exactly three render, oldest first
    (`summary 3`, `summary 4`, `summary 5`) — the cap is by recency, not by first-seen.
  - The rendered fold prompt contains the heading, the "never as evidence" framing, the seed text,
    and no leftover placeholder.
- Clean-room: n/a (Codex is Apache-2.0 and inspectable).
- Prompt change (AGENTS.md rule 8): `prompts/fold_user.md` version 1 → 2. Mock-corpus result: the
  full suite passes with the mock backend and the deterministic snapshots above. **No real-model
  evaluation exists yet** — until the M5 harness and the 10-session corpus land, this change is
  justified by construction and by unit tests, not by a measured probe score. That is a real gap, not
  a formality: the seed block could in principle distract the fold.
- Residual risk: the cap (3 summaries, 400 tokens) is a reasoned default with no measurement behind
  it, the same status as `chunk_tokens` and `state_tokens`. Revisit when the eval harness exists.
