# Feature Specification: M2 - Session discovery across stores

**Feature Branch**: `008-m2-session-discovery`
**Created**: 2026-09-10
**Status**: Stub
**Input**: docs/SCTXX-ROADMAP.md M2; docs/SPEC.md §3.2–3.5

## Scope seed

- `list`, `find`, `show`, `doctor`, full reference grammar across all stores, ambiguity exit code 3, index cache.

## Unlocked by

- No open Wayfinder ticket; unlocked when the previous milestone's exit criterion is met.

## Next step

Not approved scope. When unlocked, set `.specify/feature.json` to `specs/008-m2-session-discovery` and run
`/speckit-specify` to replace this stub with a full specification.
